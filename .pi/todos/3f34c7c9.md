{
  "id": "3f34c7c9",
  "title": "TDD: Add per-worker elapsed timers to all fleet status.sh",
  "tags": [
    "tdd",
    "fleet",
    "timer",
    "status"
  ],
  "status": "closed",
  "created_at": "2026-05-05T13:51:26.239Z"
}

## Tasks — ALL DONE ✅

1. **dag-fleet status.sh** — ✅ Added per-worker `ELAPSED` column (ctime→mtime duration)
2. **worktree-fleet status.sh** — ✅ Added per-worker `ELAPSED` column (ctime→mtime duration)
3. **autoresearch-fleet status.sh** — ✅ Added per-iteration duration in "Recent Iterations" section

### TDD Results
- `test/tdd-timers.sh`: **3/3 PASS**
- `fixtures-claude/run-all.sh`: **4/5 PASS** (L failure is pre-existing race condition)
- Topo sort regression: **PASS**

### What changed
- `skills/dag-fleet/scripts/status.sh`: Added `ELAPSED` column (10 chars) to header + per-worker row + JSON output. Uses `stat %W` (birth) → `stat %Y` (mtime) for actual session duration.
- `skills/worktree-fleet/scripts/status.sh`: Same pattern — `ELAPSED` column added between `BRANCH` and `STATUS`.
- `skills/autoresearch-fleet/scripts/status.sh`: Renamed "Recent Iteration Costs" → "Recent Iterations", added duration next to each iter's cost.
- `test/tdd-timers.sh`: New TDD test verifying all 3 fleet types show elapsed/iteration duration.
