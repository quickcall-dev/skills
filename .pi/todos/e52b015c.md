{
  "id": "e52b015c",
  "title": "TDD fixes: gitignore, AGENTS_SKILLS_DIR, kimi-for-coding",
  "tags": [
    "tdd",
    "fleet",
    "skills"
  ],
  "status": "closed",
  "created_at": "2026-05-05T12:59:11.001Z"
}

## Tasks

1. **Add `.gitignore` to fleet root** — ✅ DONE
   - Test added in `test/tdd-regression.sh`
   - Implemented in `skills/dag-fleet/scripts/launch.sh`
   - Verifies `.gitignore` created with `*.jsonl`, `*.out`, `*.out.last`

2. **`CLAUDE_SKILL_DIR` → `AGENTS_SKILLS_DIR`** — ✅ DONE
   - Test added in `test/tdd-regression.sh`
   - Updated 8 files across all skills + PR template:
     - `skills/dag-fleet/SKILL.md`
     - `skills/dag-fleet/references/quick-ref.md`
     - `skills/doc/SKILL.md`
     - `skills/worktree-fleet/SKILL.md`
     - `skills/worktree-fleet/references/quick-ref.md`
     - `skills/iterative-fleet/SKILL.md`
     - `skills/iterative-fleet/references/quick-ref.md`
     - `skills/autoresearch-fleet/SKILL.md`
     - `.github/pull_request_template.md`

3. **Use `kimi-for-coding` in pi test fixtures** — ✅ DONE
   - Test added in `test/tdd-regression.sh`
   - Updated 3 JSON files:
     - `test/fleet/dag-fleet/fixtures-pi/completion-fleet.json`
     - `test/fleet/dag-fleet/fixtures-pi/cycle-fleet.json`
     - `test/fleet/dag-fleet/fixtures-pi/dag-fleet.json`

4. **Run full test suite** — Partial
   - `fixtures-claude/run-all.sh`: 4/5 pass (L failure is pre-existing race condition)
   - `fixtures-pi/run-all.sh`: Too slow to run full suite (13 scenarios × ~35s each = >7min), but individual P2 scenario passes correctly
   - `test/tdd-regression.sh`: 3/3 pass

## Notes
- `_canonical/` is the source of truth for shared fleet libraries. `scripts/sync-lib.sh` copies canonical files into each skill's `lib/` directory. Changes to `launch.sh` are skill-specific, not in `_canonical`.
- The pre-existing L test failure (wedged-launcher fleet lock) is a race condition in the test itself where `.launch.pid` gets overwritten between the two launch invocations. Not related to these changes.
