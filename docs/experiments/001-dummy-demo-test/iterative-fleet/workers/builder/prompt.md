Write a Python script `fizzbuzz.py` that:

1. Defines `fizzbuzz(n)` — returns a list of strings for 1..n
   - Divisible by 3 and 5: "FizzBuzz"
   - Divisible by 3 only: "Fizz"
   - Divisible by 5 only: "Buzz"
   - Otherwise: the number as a string
2. Handles edge cases: n=0 returns [], negative n returns []
3. Has a main block that prints fizzbuzz(20), one per line

Save the file to /home/sagar/skills/docs/experiments/001-dummy-demo-test/iterative-fleet/workers/builder/output/fizzbuzz.py — use absolute paths.

If a previous iteration's review exists in the `iterations/` directory, read it and fix the issues noted.
