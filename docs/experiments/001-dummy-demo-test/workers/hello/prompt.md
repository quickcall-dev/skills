Create a Python script at `/home/sagar/skills/docs/experiments/001-dummy-demo-test/workers/hello/output/hello.py` that:

1. Defines a `greet(name)` function that returns `"Hello, {name}!"`
2. Has a `if __name__ == "__main__"` block that calls `greet("World")` and prints the result

Keep it simple — under 10 lines.

Save ALL output files to /home/sagar/skills/docs/experiments/001-dummy-demo-test/workers/hello/output/ — use absolute paths.
